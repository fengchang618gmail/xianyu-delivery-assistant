# PRD：闲鱼虚拟商品发货助手

## 1. 产品概述

一款面向 Windows 闲鱼虚拟商品卖家的本地桌面助手：在卖家聊天网页中识别需要卖家发货的订单，读取当前订单的宝贝信息，到飞书多维表格查找对应发货内容，并由卖家确认后发送。用户可在设置中主动切换到自动发送模式。

首个版本的体验目标是“一个安装包、一次向导、日常一键开始监控”。

## 2. 目标用户

- 自己经营闲鱼虚拟商品的 Windows 卖家。
- 需要在另一台 Windows 电脑或交付给其他卖家使用的用户。
- 使用飞书多维表格维护商品和发货模板的卖家。

## 3. 范围与原则

### MVP（必须）

- 支持 Windows 10/11。
- 一个桌面安装包，包含桌面应用和浏览器连接组件的安装引导。
- 支持 Chrome 和 Edge；用户自行登录闲鱼卖家网页。
- 首次运行向导自动检查浏览器组件、闲鱼登录、飞书授权和商品库字段。
- 连接飞书多维表格，优先按“商品 ID”匹配；闲鱼页面无法取得商品 ID 时，才按“商品标题”精确匹配并读取“发货内容”。
- 识别“等待卖家发货”这一明确订单状态；无法可靠识别时不创建发送任务。
- 默认“确认后发送”：显示宝贝、匹配的模板和发货内容，用户可编辑并确认。
- 可切换至“自动发送”；切换前需有明确风险提示，且提供全局暂停开关。
- 首次安全测试：验证各连接与模板匹配，并默认只预览、不实际发送；真实测试发送必须由用户确认。
- 本地去重、操作记录、异常通知和一键重新检测。

### 不在 MVP 范围

- macOS、Linux、移动端。
- 多店铺集中管理、团队成员权限、云端托管。
- 自动创建或修改闲鱼商品、订单、售后与退款操作。
- 解析或保存闲鱼账户密码、支付信息、买家敏感信息。

## 4. 关键业务规则

1. 仅当订单状态明确为“等待卖家发货”时才进入发货流程。
2. 商品匹配优先使用闲鱼商品 ID 与飞书“商品 ID”的精确匹配；仅当闲鱼页面无法取得商品 ID 时，才使用飞书“商品标题”与闲鱼宝贝标题的标准化精确匹配（忽略首尾空格及连续空白）。任一匹配未命中或命中多行均视为异常。
3. 无匹配、匹配多条、模板为空、订单状态变化、网页离线或出现验证提示时，停止发送并告知用户。
4. 每一笔任务以“订单/会话标识 + 状态事件 + 发货内容摘要”去重；发送结果未在页面确认前不得标记完成。
5. 发货记录默认只保存在该电脑；日志对发货内容、链接、提取码等敏感字段进行脱敏。
6. 闲鱼登录态和飞书访问凭据仅保存在本机系统安全凭据存储，且不包含在迁移配置或安装包中。
7. 网页自动化须以平台许可、授权能力和实际页面规则为前提；不采用抓包、伪造请求或绕过验证的方式。

## 5. 用户体验设计

### 安装与首次使用

1. 用户下载并运行一个 Windows 安装包。
2. 应用自动检测并引导启用浏览器连接组件；浏览器要求确认扩展时，仅需用户点击一次“添加/启用”。
3. 向导依次完成：闲鱼登录检查、飞书授权、商品库选择、字段映射、预览测试。
4. 每一步只有一个主操作；成功步骤自动跳过，失败时提供可理解的修复按钮。
5. 向导完成后显示“开始监控”。

### 日常使用

- 主界面只显示连接状态、当前发送模式、开始/暂停监控、待确认任务和最近记录。
- 首次默认确认模式。用户可在设置内立即切换确认模式与自动发送模式。
- 已完成配置后不重复展示教程；只在登录失效、授权到期、组件被关闭或数据映射失效时定位到对应修复步骤。

## 6. 数据模型（概念）

### 飞书多维表格商品库

- `商品 ID`：优先匹配字段，须唯一且与闲鱼商品 ID 一致。
- `商品标题`：商品 ID 无法取得时的兜底匹配字段，须与闲鱼宝贝标题一致。
- `发货内容`：对应商品的发送模板。

不要求用户维护其他字段。若多维表格中有重复商品 ID 或重复商品标题，系统将停止该笔任务并提示用户先在飞书中消除重复项。

### 本地任务记录

- 订单/会话标识、商品识别结果、发货内容摘要、处理模式、处理状态、时间、失败原因、脱敏结果摘要。

## 7. 用户故事

| ID | 用户故事 | 优先级 | 类型 |
|---|---|---|---|
| US-001 | 作为卖家，我只运行一个安装包就能安装并完成浏览器连接，以便无需技术配置即可开始使用。 | P0 | 全栈 |
| US-002 | 作为首次用户，我可按单步骤向导连接闲鱼和飞书，并运行安全测试，以便确认配置无误。 | P0 | 全栈 |
| US-003 | 作为卖家，当有待发货订单时，我可看到匹配的商品与发货内容并编辑确认后发送，以便避免发错虚拟商品。 | P0 | 全栈 |
| US-004 | 作为卖家，我可随时将确认模式切换为自动发送或暂停监控，以便按风险偏好处理订单。 | P0 | 全栈 |
| US-005 | 作为卖家，我可查看去重后的发货记录和失败原因，以便追踪订单并处理异常。 | P0 | 全栈 |
| US-006 | 作为迁移到新电脑的用户，我可重新安装并授权自己的闲鱼与飞书账号，以便不迁移任何敏感凭据。 | P1 | 全栈 |

## 8. 验收标准

- 新电脑从下载安装包到完成首次安全测试，按向导可独立完成，无需命令行或手动配置文件。
- 在默认确认模式下，系统绝不在用户确认前发送发货内容。
- 每次已发送且被网页确认的订单只生成一条完成记录。
- 飞书没有唯一匹配结果时，系统不会发送任何内容。
- 应用在任一异常下提供暂停和人工处理入口，不能静默重复发送。
- 用户可在设置中切换模式，切换后的下一笔任务立即按新模式处理。

## 9. 后续阶段

- 获取并接入平台允许的官方/授权消息与履约能力后，替换网页侧适配而复用商品库、任务、去重与体验层。
- 支持组织配置、签名发布、扩展商店分发和可选的团队管理。
