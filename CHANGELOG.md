# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.0] - 2026-09-09

首个公开版本。

### 功能

- **桌面应用（Tauri 2 + Rust）**
  - 本地 SQLite 存储：配置、商品缓存、发货任务、审计事件。
  - 飞书 OAuth 授权（本地回调 `127.0.0.1:15731`）+ 令牌自动续期（`offline_access`）。
  - 飞书多维表格只读读取，三列字段校验（`商品 ID` / `商品标题` / `发货内容`），字段名容错匹配。
  - 任务状态机：已发现 → 匹配中 → 待确认 / 待自动发送 → 待发送 → 已完成 / 异常 / 已取消 / 已忽略。
  - 匹配规则：商品 ID 精确匹配优先，标准化标题精确匹配兜底；**零条或多条匹配、内容为空一律不发送**。
  - 自动模式：60 秒二次校验，订单状态变化或商品不一致时降级为人工确认；一次性批准令牌，超时不盲目重发。
  - Native Messaging 宿主：命名管道 `\\.\pipe\xianyu-delivery-assistant-nm`（行分隔 JSON，`v:1` 协议），hello 令牌认证。
  - 脱敏审计日志；「清除全部记录」一键擦除数据库与凭据。
- **浏览器扩展（Manifest V3，Chrome / Edge）**
  - 仅运行在 `seller.goofish.com`；固定扩展 ID `lkfdpfllnpejncdiggdbiffiebombajd`。
  - 提取商品 ID、商品标题、订单号；填入内容并发送，回传发送证据。
  - `chrome.alarms`（30 秒）周期重连，规避 Service Worker 休眠导致失联。
- **前端（React）**
  - 6 步首次配置向导：浏览器连接 → 闲鱼登录 → 飞书应用 → 飞书授权 → 商品库校验 → 安全测试预览。
  - 日常工作台：连接状态、手动 / 自动模式切换、暂停 / 恢复、待确认任务卡片（内容可编辑）、最近任务、脱敏日志。

### 修复（开发期累积）

- 修复飞书令牌请求失败：`token_request` 对传输层错误自动重试 3 次，并在错误信息中给出错误类别与完整原因链。
- 修复扩展上行消息缺少 `v:1` 协议字段导致所有心跳与订单事件被静默丢弃的问题。
- 修复认证成功的 Hello 被管道服务器认证分支消费、`ext-conn` 事件永不触发的问题。
- 修复浏览器关闭后 nmhost 阻塞在管道读循环不退出的问题（stdin EOF 即退出）。
- 修复安装时 `Error opening file for writing nmhost.exe`（安装钩子先结束进程再写入）。
- 修复 Windows 凭据管理器单条 Blob 上限（2560 字节）导致飞书令牌写入失败：改为 DPAPI 加密后存本地文件。
- 修复知识库形态分享链接（`/wiki/<节点token>`）无法直接调用多维表格 API 的问题（先换取 `obj_token`）。
- 修复列名细微差异（如「商品ID」缺空格）导致飞书整体拒绝请求的问题（改为先读实际字段名再容错匹配）。
- 向导内所有异步按钮改为点击瞬间反馈（「xx中…」并禁用）。

### 已知限制

- 扩展的 DOM 选择器尚未在真实闲鱼页面完整实测，首次使用可能需要按实际页面结构微调。
- 飞书端到端流程需要用户完成授权后才能真实验证。
- 浏览器扩展无法静默安装，需手动加载一次。
- 仅支持 Windows 10 / 11 64 位。
